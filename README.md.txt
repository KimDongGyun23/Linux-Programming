2019203002 김동균
리눅스 활용실습 프로젝트1 과제

<1> 2019203002_KimDongGyun 폴더 내의 파일들 내역
 

<2> 2019203002_KimDongGyun.sh의 주요 내용
 
출력할 내용들 및 방향키로 입력될 문자들 변수로 지정
 
사용할 텍스트 파일들 생성
	1) infor.txt – 로그인 할 때 입력되는 아이디와 비밀번호를 기록할 텍스트파일
	2) exinfor.txt – 회원가입에 사용된 아이디와 비밀번호를 기록할 텍스트파일
	3) delete.txt – 회원탈퇴 할 때 위 파일들에 덮어씌울 텍스트파일 (빈 파일)
 
	while문에서 사용될 변수 지정
 
	키보드에서 어떠한 키가 눌려졌을 때 join버튼이 빨간색으로 출력됨
 
	플레이어에게서 3비트를 입력받고 input 변수에 저장
   
플레이어가 엔터를 입력했을 때, 저장되어있는 SELECTED 변수의 값에 따라서 다른 함수들이 실행됨
 
	입력된 키가 방향키의 상하좌우 버튼이라면 그에 따라서 SELETED의 값이 변함 
오른쪽 방향키 : +1
아래쪽 방향키 : +2
왼쪽 방향키 : -1 
위쪽 방향키 : -2
최댓값과 최솟값을 설정하여 SELETED가 최댓값과 최솟값을 넘어가지 못하도록 하였다.
	이후 SELECTED의 값에 따라서 다른 화면들이 출력되도록 설정

<3> mainscreen.sh 의 주요 내용
 
메인화면에 출력될 내용들. 변수들의 배경색을 빨간색과 파란색으로 지정하여 메인화면에 버튼 모양이 구현되도록 하였다. 
<4> joinscreen.sh 의 주요 내용
 
  
joinwhile.sh에 사용될 함수들. 변수들의 배경색을 빨간색과 파란색으로 지정하여 버튼 모양이 구현되도록 하였다.

<5> joinwhile.sh 의 주요 내용
 
	while문에서 사용될 변수 지정
 
	키보드에서 어떠한 키가 눌려졌을 때 ID버튼이 빨간색으로 출력됨
 
	플레이어에게서 3비트를 입력받고 input5 변수에 저장
 
플레이어가 엔터키를 입력했을 경우, INDEX5에 저장된 값이
1일 경우 - loginid3을 입력 받는다. ID라는 글자대신 입력 받은 문자열을 출력하기 위해서 ID변수를 수정한다.
2일 경우 - loginpw3을 입력 받고, PW변수를 수정한다. 이후 loginid3, loginpw3, WIN, LOSE 값들을 infor.txt에 기록한다.
3일 경우 - infor.txt와 exinfor.txt의 내용을 비교하여 다른 내용을 diff.txt에 덮어씌운다. 이때 두 파일 내용이 같다면 diff.txt에는 아무 내용도 작성되지 않는다. 이를 통해 diff.txt에 어떠한 내용이라도 있다면 프로그램을 종료하고, 아무 내용도 없다면 ID와 PW변수를 다시 원상태로 초기화하고 join2while 함수를 실행한다.
	4일 경우 – 프로그램을 종료한다. 
 
입력된 키가 방향키의 상하좌우 버튼이라면 그에 따라서 INDEX5의 값이 변함
오른쪽 방향키 : +1
아래쪽 방향키 : +1
왼쪽 방향키 : -1
위쪽 방향키 : -1
최댓값과 최솟값을 설정하여 INDEX5가 최댓값과 최솟값을 넘어가지 못하도록 하였다.
	이후 INDEX5의 값에 따라서 다른 화면들이 출력되도록 설정

<6> join2while.sh 의 주요 내용
 
	앞부분의 맥락은 joinwhile.sh와 동일하다.
 
INDEX6이 3일 때, infor.txt와 exinfor.txt를 비교하여 다른 내용을 diff.txt에 덮어씌운다. 이때 diff.txt에 어떠한 내용이라도 있다면 프로그램을 종료하고, 아무 내용도 없다면 SUCCESS를 출력하고 끝이 난다. 이후 내용은 joinwhile.sh와 동일하다. 입력된 키가 방향키의 상하좌우 버튼이라면 그에 따라서 INDEX6의 값이 변한다.

<7> signupscreen.sh의 주요 내용
 
sign in 화면에 출력될 내용들. 변수들의 배경색을 빨간색과 파란색으로 지정하여 화면의 버튼 모양이 구현되도록 하였다.

<8> signwhile.sh의 주요 내용
 
	앞부분의 맥락은 joinwhile.sh와 동일하다.
 
 
플레이어가 엔터키를 입력했을 경우, INDEX에 저장된 값이 
1일 경우 – loginid을 입력 받는다. ID라는 글자대신 입력 받은 문자열을 출력하기 위해서 ID변수를 수정한다.
2일 경우 - infort.txt와 exinfor.txt의 내용을 비교하여 다른 내용을 diff.txt에 덮어씌운다. 만약 diff.txt에 어떠한 내용이라도 있다면, “회원가입 가능”이라는 문구를 출력하고 종료한다. 내용이 존재하지 않는다면, “같은 ID 존재”라는 문구를 출력하고 종료한다.
3일 경우 – loginpw을 입력 받고, PW변수를 수정한다. 이후 loginid, loginpw, WIN, LOSE 값들을 infor.txt에 기록한다.
5일 경우 - loginid, loginpw, WIN, LOSE의 값을 exinfor.txt에 기록하고 종료한다. 
6일 경우 - 프로그램을 종료한다.
 
2019203002_KimDongGyun.sh 에서와 마찬가지로 입력되는 방향키에 따라서 INDEX의 값이 변화한다. 최댓값과 최솟값을 설정하여 INDEX가 최댓값과 최솟값을 넘어가지 못하도록 하였다. 이후 INDEX의 값에 따라서 화면을 출력한다.

<9> signoutscreen.sh의 주요 내용
 
sign out 화면에 출력될 내용들. 변수들의 배경색을 빨간색과 파란색으로 지정하여 화면의 버튼 모양이 구현되도록 하였다.
<10> signoutwhile.sh의 주요 내용
 
	앞부분의 맥락은 joinwhile.sh와 동일하다.
 
플레이어가 엔터키를 입력했을 경우, INDEX4에 저장된 값이
1일 경우 – loginid1을 입력 받는다. ID라는 글자대신 입력 받은 문자열을 출력하기 위해서 ID변수를 수정한다.
2일 경우 – loginpw1을 입력 받고, PW변수를 수정한다. 이후 loginid1, loginpw1, WIN, LOSE 값들을 infor.txt에 기록한다. infor.txt와 exinfor.txt의 내용을 비교하여 다른 내용을 diff.txt에 덮어씌운다. 이때 두 파일 내용이 같다면 diff.txt에는 아무 내용도 작성되지 않는다.
3일 경우 - diff.txt에 어떠한 내용이라도 있다면 프로그램을 종료한다. 아무 내용도 없다면 delete.txt의 내용을 infor.txt와 exinfor.txt에 덮어씌우고 종료한다. 이때, delete.txt에는 어떠한 내용도 존재하지 않는다. 따라서 infor.txt와 exinfor.txt에는 아무 내용도 없는 파일이 덮어 씌워지는 것이다.
4일 경우 – 프로그램을 종료한다.
 
joinwhile.sh 에서와 마찬가지로 입력되는 방향키에 따라서 INDEX4의 값이 변화한다. 최댓값과 최솟값을 설정하여 INDEX4가 최댓값과 최솟값을 넘어가지 못하도록 하였다. 이후 INDEX4의 값에 따라서 화면을 출력한다.
